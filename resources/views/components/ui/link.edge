<a
  class="
    w-fit transition-colors bg-primary flex flex-row items-center text-sm text-white hover:bg-primary/95 active:bg-primary/90 gap-3
    {{className ? className : ''}}
    {{variant === 'sm' ? 'px-3 py-2  rounded-lg' : 'py-4 px-6 rounded-xl'}}
  "
  href="{{path}}"
>{{{ await $slots.main() }}}</a>
